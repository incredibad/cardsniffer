
# Encryption key for secrets stored in the settings table (e.g. eBay Cert ID).
# Generate one with:
#   python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Keep this out of git — losing it makes any encrypted settings unrecoverable.
SECRET_KEY=
